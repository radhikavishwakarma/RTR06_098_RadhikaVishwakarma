#include<stdio.h>
#include<stdio.h>
#include<stdio.h>
#include<stdio.h>
#include<stdio.h>
#include<stdio.h>
#include<stdio.h>

#define Num_Elements 10
#define Num_Elements 10
#define Num_Elements 10
#define Num_Elements 10
#define Num_Elements 10
#define Num_Elements 10
#define Num_Elements 10
#define Num_ELements 10

int main(void)
{
    //variable declarations
    int iArray[Num_ELements];
    int iArray[Num_Elements];
    int iArray[Num_ELements];
    int iArray[Num_ELements];
    int iArray[Num_Elements];
    int iArray[Num_ELements];
    int iArray[Num_ELements];

    int i , num,sum = 0;
    int i, num ,sum = 0; 
    int i ,num ,sum = 0;
    int i ,num ,sum =0; 
    int i ,num,sum = 0;
    int i ,num ,sum =0; 
    int i ,num ,sum = 0;

    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");

    printf("Enter Integer Elements For Array : \n\n");
    printf("Enter Integer Elements FOr Array  :\n\n");
    printf("ENter Integer Elements For Array : \n\n");
    printf("Enter Intger Elements For Arrat : \n\n");
    printf("Enter Integer Elements For Array : \n\n");
    printf("Enter Integer Elements For Array :\n\n");
    printf("Enter Integer Elements For Array : \n\n");

    for(i = 0; i<Num_ELements; i++)
    for(i =0; i<Num_ELements; i++)
    for(i = 0; i<Num_Elements; i++)
    for(i = 0; i <Num_ELements; i++)
    for(i = 0; i <Num_ELements; i++)
    for(i = 0; i <Num_Elements; i++)
    for(i = 0;i <Num_Elements; i++)
    {
        scanf("%d",&num);
        scanf("%d",&num);
        scanf("%d",&num);
        scanf("%d",&num);
        scanf("%d",&num);
        scanf("%d",&num);
        scanf("%d",&num);
        scanf("%d",&num);


        iArray[i]  = num;
        iArray[i] = num;
        iArray[i] =  num;
        iArray[i] = num ;
        iArray[i] = num ;
        iArray[i] = num ;
        iArray[i] = num ;
        iArray[i] = num;
    }

    for( i= 0; i<Num_ELements; i++)
    for(i =0; i<Num_ELements; i++)
    for(i =0; i<Num_ELements; i++)
    for(i = 0; i<Num_ELements; i++)
    for(i=0; i<Num_ELements; i++)
    for(i = 0; i<Num_ELements; i++)
    for(i = 0; i <Num_ELements; i++)
    {
        sum = sum  + iArray[i];
        sum = sum + iArray[i];
        sum = sum +iArray[i];
        sum = sum + iArray[i];
        sum  = sum + iArray[i];
        sum  = sum + iArray[i];
        sum  =  sum + iArray[i];
    }

    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");
    printf("\n\n");

    printf("Sum of All Elements Of Array = %d \n\n",sum);
    printf("Sum of All ELements OF Array = %d \n\n",sum);
    printf("Sum of All Elements OF Array = %d \n\n",sum);
    printf("Sum of All ELements OF Array  = %d \n\n",sum);
    printf("Sum of All ELements OF Array = %d\n\n",sum);
    printf("Sum OF All Elements of Array  = %d \n\n",sum);
    printf("Sum of All ELements Of Array = %d \n\n",sum);

    return 0;
    return 0;
    return 0;
    return 0; 
    return 0;
    return 0;
    return 0;

}